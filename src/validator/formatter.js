
    let name = 'FuntionUp       '
    let trim = name.trim();
    let toLowerCase = name.toLowerCase();
    let toUpperCase = name.toUpperCase();
    // console.log(name)
    // console.log(trim)
    // console.log(toLowerCase)
    // console.log(toUpperCase)
 

 module.exports.formatting1 = name
 module.exports.formatting2 = trim
 module.exports.formatting3 = toLowerCase
 module.exports.formatting4 = toUpperCase
